var assert = require('chai').assert;

describe('Sum', function () {
  it('should return 0 with zero elements', function () {
    assert.equal(5, 5);
  });
  it('should return 10', function () {
    assert.equal(3, 3);
  });
});